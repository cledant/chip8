#include "emu_loop.h"

#include <stdio.h>

int
emu_fetch(char const **err)
{
    *err = "TODO emu_fetch";
    return (1);
}

int
emu_decode(char const **err)
{
    *err = "TODO emu_decode";
    return (1);
}

int
emu_execute(char const **err)
{
    *err = "TODO emu_execute";
    return (1);
}

int
emu_loop(char const **err)
{
    *err = "TODO emu_loop";
    return (1);
}
